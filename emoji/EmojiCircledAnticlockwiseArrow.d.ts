import type { Vnode } from 'mithril'

declare const EmojiCircledAnticlockwiseArrow: { view: (vnode: Vnode<{ size?: number; [key: string]: any }>) => any }
export default EmojiCircledAnticlockwiseArrow
