import type { Vnode } from 'mithril'

declare const EmojiPinchedFingersMediumSkinTone: { view: (vnode: Vnode<{ size?: number; [key: string]: any }>) => any }
export default EmojiPinchedFingersMediumSkinTone
